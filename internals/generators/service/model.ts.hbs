
/**
 *
 *
 * @export
 * @class {{ properCase ModelName }}
 */
export class {{ properCase ModelName }} {
    protected id: string;

    constructor(o?: any) {
        const { id = `1-${Math.random()}` } = o || ({} as any);

        this.id = id;
    }
}

export type {{ properCase ModelName }}s = {{ properCase ModelName }}[];

export default {{ properCase ModelName }};
