import { translations } from 'locales/translations';
import { _t } from 'utils/messages';

export const messages = {
    title: _t(translations.Program.programDevices.Title, 'Devices'),
    subTitle: _t(translations.Program.programDevices.SubTitle, 'Select devices to apply changes to'),
    tableHeader: _t(translations.Program.programDevices.TableHeader, 'Device Name'),
};
