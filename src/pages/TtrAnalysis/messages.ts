import { translations } from 'locales/translations';
import { _t } from 'utils/messages';

export const messages = {
    ttrAnalysisPageTitle: _t(translations.TtrAnalysis.PageTitle, 'TtrAnalysis '), // default value
    ttrAnalysisTitle: _t(translations.TtrAnalysis.Title, 'TtrAnalysis '), // default value
};
