/**
 *
 *
 * @export
 * @class Voltage
 */
export interface Voltages {
    minimum?: [];
    maximum?: [];
    actual?: [];
    occurredOn?: [];
    deviceNames?: [];
    studIds?: [];
    studTypes?: [];
    week?: [];
    lines?: [];
}

// export type Voltages = Voltage[];

export default Voltages;
